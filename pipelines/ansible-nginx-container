#!/usr/bin/env groovy

pipeline {
    // run jobs on agents with the label swarm
    agent { label 'swarm' }
//    agent any 
    stages {
        stage('build') {
            steps {
                sh 'env'
            }
        }
    }
}
