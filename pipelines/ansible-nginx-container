#!/usr/bin/env groovy

pipeline {
    agent { label 'my-defined-label' }
//    agent any 
    stages {
        stage('build') {
            steps {
                sh 'env'
            }
        }
    }
}
